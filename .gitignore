# Runtime / cache
.venv/
__pycache__/
*.pyc
.pytest_cache/
build/
dist/
data/
logs/
*.log
tools/_out/

# DSH AgentTeams runtime state (not published)
.agent-teams/

# One-shot credential files: never commit
.gh_token
*.token
tokens.txt

# DSH vision-router artifacts / temp screenshots (local run traces, never publish)
.dsh-vision-router/
*.present.png

# 执行报告：只存本地，不入库（元机制记录，不属于产品代码）
执行报告-首次运行.md
执行报告-变更记录.md
